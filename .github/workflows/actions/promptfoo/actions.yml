name: commit promptfoo result to pull request
description: |
  This action commits the result of the promptfoo evaluation to the pull request.
inputs:
  token:
    description: GitHub token with issue, comment, and label read/write permissions
    required: true

runs:
  using: 'node16'
  main: 'index.js'
