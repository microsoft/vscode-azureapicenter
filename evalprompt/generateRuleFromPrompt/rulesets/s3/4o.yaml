rules:
  get-paths-with-id-404:
    description: "GET requests with an ID at the end of their path (e.g., /teams/{teamID}) must define a 404 response."
    severity: error
    given: "$.paths[*][get]"
    then:
      field: responses
      function: truthy
      functionOptions:
        property: "404"
